services:
  - type: web
    name: yoga-wellness-rag-backend
    runtime: node
    plan: free
    buildCommand: npm ci --prefix backend
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: MONGO_URI
        sync: false
